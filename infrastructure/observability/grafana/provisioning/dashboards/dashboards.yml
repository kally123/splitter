apiVersion: 1

providers:
  - name: 'Splitter Dashboards'
    orgId: 1
    folder: 'Splitter'
    folderUid: 'splitter'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
